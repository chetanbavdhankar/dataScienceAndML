#!/usr/bin/env python
"""
  The code serves only for educational purposes so it
  is not optimized in view of speed or numerical calculations.
  For the real problems you should always use algorithms.
  from known,  well tested libraries, wherever possible.
  Author: Wojtek Krzemien
  Date: 10.04 2018
"""

def normalizeData(X):
  """
    Function performs the features scaling, The normalization should be done
    feature-wise x' = (x-mean(x))/sigma, where sigma is standard deviation
    Args:
      x(list): list of feature points that should be normalized 
    Returns:
     list: of normalize feature points 
  """
  # TO IMPLEMENT
  return -1
